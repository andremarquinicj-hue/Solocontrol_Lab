'use client';
import Link from 'next/link';import { useEffect,useState } from 'react';import { listSamples } from '@/lib/store';import { Sample } from '@/lib/types';
export default function ReportsPage(){const[samples,setSamples]=useState<Sample[]>([]);useEffect(()=>{listSamples().then(setSamples)},[]);return <div className="page-stack"><section className="page-heading"><div><span className="eyebrow">RELATÓRIOS</span><h1>Central de relatórios</h1><p>Acesse a amostra e use a versão de impressão gerencial.</p></div></section><section className="panel"><div className="cards-list">{samples.map(s=><Link href={`/amostras/${s.id}`} className="sample-card" key={s.id}><div><b>{s.labelBase}</b><small>{s.workName}</small></div><div><span>Status</span><b>{s.status}</b></div><div><span>Rupturas</span><b>{s.ruptures.filter(r=>r.status==='concluido').length}/{s.ruptures.length}</b></div><span className="text-link">Abrir relatório</span></Link>)}</div></section></div>}
